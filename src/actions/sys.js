export const setPath = (path) => ({
    type: 'SET_PATH',
    path
});

export const removePath = () => ({
    type: 'REMOVE_PATH'
});

export const setTitle = (title) => ({
    type: 'SET_TITLE',
    title
});